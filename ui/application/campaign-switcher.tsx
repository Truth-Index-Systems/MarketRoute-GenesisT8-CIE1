export interface CampaignOption {campaignId:string;name:string;workflowState:string;}
export function CampaignSwitcher({campaigns,current,action}:{campaigns:CampaignOption[];current:string|null;action:string}){if(campaigns.length===0)return null;return <form className="mr-campaign-switcher" method="get" action={action}><label><span>Campaign</span><select name="campaign" defaultValue={current??campaigns[0].campaignId}>{campaigns.map((c)=><option value={c.campaignId} key={c.campaignId}>{c.name} · {c.workflowState}</option>)}</select></label><button className="mr-button mr-button--secondary" type="submit">Load</button></form>}
