name: AWS V0 Build 3 Schema Audit

on:
  workflow_dispatch:
  push:
    branches:
      - aws-v0
    paths:
      - 'database/aws/**'
      - 'supabase/migrations/**'
      - '.github/workflows/aws-v0-build3-schema-audit.yml'

permissions:
  contents: read

concurrency:
  group: aws-v0-build3-schema-audit
  cancel-in-progress: false

jobs:
  schema-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: marketroute_build3
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d marketroute_build3"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      PGHOST: 127.0.0.1
      PGPORT: '5432'
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: marketroute_build3

    steps:
      - name: Checkout canonical aws-v0 branch
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Syntax-check Build 3 compiler and resolver tooling
        run: |
          node --check database/aws/compile-canonical-baseline.mjs
          node --check database/aws/test-compiler-hardening.mjs
          node --check database/aws/apply-build3-dispositions.mjs
          node --check database/aws/test-build3-dispositions.mjs
          node --check database/aws/apply-build3-final-disposition-closure.mjs
          node --check database/aws/test-build3-final-disposition-closure.mjs
          node --check database/aws/build3-final-object-replay.mjs
          node --check database/aws/verify-build3-final-object-dump.mjs
          node --check database/aws/test-build3-final-object-resolver.mjs

      - name: Prove compiler hardening invariants
        run: node database/aws/test-compiler-hardening.mjs

      - name: Prove disposition invariants
        run: node database/aws/test-build3-dispositions.mjs

      - name: Prove final disposition closure invariants
        run: node database/aws/test-build3-final-disposition-closure.mjs

      - name: Prove final-object resolver invariants
        run: node database/aws/test-build3-final-object-resolver.mjs

      - name: Run Build 3 hardened audit
        run: node database/aws/compile-canonical-baseline.mjs

      - name: Apply Build 3 canonical dispositions
        run: node database/aws/apply-build3-dispositions.mjs

      - name: Apply reviewed final disposition closure
        run: node database/aws/apply-build3-final-disposition-closure.mjs

      - name: Build final-object reference replay
        run: node database/aws/build3-final-object-replay.mjs

      - name: Resolve final PostgreSQL objects in disposable PostgreSQL 16
        run: |
          psql --set=ON_ERROR_STOP=1 --file=database/aws/build3_final_object_replay.sql

      - name: Prove fresh-data and final-configuration invariants
        run: |
          psql --set=ON_ERROR_STOP=1 <<'SQL'
          DO $gate$
          DECLARE
            v_growth_enabled boolean;
            v_growth_limit integer;
            v_scale_limit integer;
            v_employment_policy text;
            v_role_policy text;
            v_channel_policy text;
          BEGIN
            IF EXISTS (SELECT 1 FROM public.organisations) THEN
              RAISE EXCEPTION 'BUILD3_HISTORICAL_ORGANISATION_DATA_PRESENT';
            END IF;
            IF EXISTS (SELECT 1 FROM public.companies) THEN
              RAISE EXCEPTION 'BUILD3_HISTORICAL_COMPANY_DATA_PRESENT';
            END IF;
            IF EXISTS (SELECT 1 FROM public.claims) THEN
              RAISE EXCEPTION 'BUILD3_HISTORICAL_CLAIM_DATA_PRESENT';
            END IF;
            IF EXISTS (SELECT 1 FROM public.opportunities) THEN
              RAISE EXCEPTION 'BUILD3_HISTORICAL_OPPORTUNITY_DATA_PRESENT';
            END IF;
            IF to_regclass('public.marketroute_v1_migration_batches') IS NOT NULL
               OR to_regclass('public.marketroute_v1_migration_id_map') IS NOT NULL
               OR to_regclass('public.marketroute_v1_migration_rejections') IS NOT NULL
               OR to_regclass('public.marketroute_v1_migration_audit_events') IS NOT NULL THEN
              RAISE EXCEPTION 'BUILD3_V1_ETL_OBJECT_PRESENT';
            END IF;

            SELECT enabled INTO v_growth_enabled
            FROM public.genesis_growth_settings
            WHERE singleton=true;
            IF v_growth_enabled IS DISTINCT FROM false THEN
              RAISE EXCEPTION 'BUILD3_DEMAND_DRIVEN_GENESIS_CONFIGURATION_DRIFT';
            END IF;

            SELECT active_market_limit INTO v_growth_limit
            FROM public.marketroute_plan_catalog WHERE plan_code='GROWTH';
            SELECT active_market_limit INTO v_scale_limit
            FROM public.marketroute_plan_catalog WHERE plan_code='SCALE';
            IF v_growth_limit IS DISTINCT FROM 3 OR v_scale_limit IS DISTINCT FROM 10 THEN
              RAISE EXCEPTION 'BUILD3_PLAN_ACTIVE_MARKET_CONFIGURATION_DRIFT';
            END IF;

            SELECT policy_key INTO v_employment_policy
            FROM public.truth_claim_policy_bindings
            WHERE subject_type='PERSON' AND claim_key='employment.current';
            SELECT policy_key INTO v_role_policy
            FROM public.truth_claim_policy_bindings
            WHERE subject_type='PERSON' AND claim_key='role.current';
            SELECT policy_key INTO v_channel_policy
            FROM public.truth_claim_policy_bindings
            WHERE subject_type='CHANNEL' AND claim_key='ownership.current';
            IF v_employment_policy IS DISTINCT FROM 'PERSON_CURRENT_EMPLOYMENT_V1'
               OR v_role_policy IS DISTINCT FROM 'PERSON_CURRENT_ROLE_V1'
               OR v_channel_policy IS DISTINCT FROM 'CHANNEL_OWNERSHIP_CURRENT_V1' THEN
              RAISE EXCEPTION 'BUILD3_TRUTH_POLICY_BINDING_CONFIGURATION_DRIFT';
            END IF;
          END;
          $gate$;
          SQL

      - name: Dump resolved schema only
        run: |
          pg_dump \
            --schema-only \
            --no-owner \
            --no-privileges \
            --exclude-schema=auth \
            --file=database/aws/build3_final_object_reference_schema.sql \
            "$PGDATABASE"

      - name: Verify resolved final-object reference schema
        run: node database/aws/verify-build3-final-object-dump.mjs

      - name: Validate generated audit, dispositions, and final-object manifests
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const audit = JSON.parse(fs.readFileSync('database/aws/build3_schema_audit.json', 'utf8'));
          const schemaManifest = JSON.parse(fs.readFileSync('database/aws/0001_marketroute_aws_schema_manifest.json', 'utf8'));
          const disposition = JSON.parse(fs.readFileSync('database/aws/build3_disposition_audit.json', 'utf8'));
          const dispositionManifest = JSON.parse(fs.readFileSync('database/aws/0001_marketroute_aws_disposition_manifest.json', 'utf8'));
          const finalDisposition = JSON.parse(fs.readFileSync('database/aws/build3_final_disposition_audit.json', 'utf8'));
          const finalManifest = JSON.parse(fs.readFileSync('database/aws/0001_marketroute_aws_final_disposition_manifest.json', 'utf8'));
          const reviewedLedger = JSON.parse(fs.readFileSync('database/aws/BUILD3-REVIEWED-DISPOSITIONS.json', 'utf8'));
          const replay = JSON.parse(fs.readFileSync('database/aws/build3_final_object_replay_manifest.json', 'utf8'));
          const finalObjects = JSON.parse(fs.readFileSync('database/aws/build3_final_object_resolution_manifest.json', 'utf8'));

          if (audit.build !== 'AWS-V0-BUILD-3') throw new Error('unexpected audit build');
          if (audit.format_version !== 2 || audit.mode !== 'hardened-audit') throw new Error('hardened audit v2 required');
          if (!Number.isInteger(audit.migration_count) || audit.migration_count < 1) throw new Error('no migrations inventoried');
          if (schemaManifest.build !== 'AWS-V0-BUILD-3' || schemaManifest.format_version !== 2) throw new Error('unexpected schema manifest format');
          if (schemaManifest.prohibited_historical_data_import !== true) throw new Error('historical data prohibition missing');
          if (!schemaManifest.excluded_migrations.includes('0019_v1_evidence_migration.sql')) throw new Error('V1 ETL exclusion missing');

          const v1 = schemaManifest.source_migrations.find((m) => m.file === '0019_v1_evidence_migration.sql');
          if (!v1 || v1.canonical_candidate !== false || v1.disposition !== 'EXCLUDE_V1_ETL') {
            throw new Error('V1 ETL is not structurally excluded from canonical candidates');
          }
          if (audit.final_object_candidates.some((o) => o.final_source?.file === '0019_v1_evidence_migration.sql')) {
            throw new Error('V1 ETL object leaked into canonical candidate set');
          }

          if (disposition.build !== 'AWS-V0-BUILD-3' || disposition.mode !== 'canonical-disposition') throw new Error('unexpected disposition audit');
          if (disposition.raw_unresolved_count !== audit.unresolved_count) throw new Error('disposition input does not match hardened unresolved set');
          if (disposition.resolved_decisions + disposition.unresolved_count !== disposition.raw_unresolved_count) throw new Error('disposition accounting mismatch');
          if (dispositionManifest.prohibited_historical_data_import !== true) throw new Error('disposition historical-data prohibition missing');

          const identity = dispositionManifest.identity_boundary;
          if (identity?.version !== 'AWS-V0-INTERNAL-IDENTITY-1') throw new Error('AWS internal identity boundary missing');
          if (identity.internal_user_relation !== 'public.marketroute_users') throw new Error('internal user relation mismatch');
          if (identity.database_actor_contract !== 'EXPLICIT_INTERNAL_USER_UUID') throw new Error('explicit actor UUID contract missing');
          if (identity.trusted_backend_contract !== 'SERVER_SIDE_DATA_API_ONLY') throw new Error('trusted backend contract missing');
          if (identity.browser_database_access !== false || identity.supabase_auth_schema_allowed !== false || identity.supabase_roles_allowed !== false || identity.request_jwt_session_state_allowed !== false) {
            throw new Error('Supabase/browser identity dependency leaked into AWS boundary');
          }

          if (reviewedLedger.build !== 'AWS-V0-BUILD-3' || reviewedLedger.format_version !== 1 || reviewedLedger.entries.length !== 23) {
            throw new Error('expected exactly 23 reviewed final disposition entries');
          }
          if (finalDisposition.build !== 'AWS-V0-BUILD-3' || finalDisposition.mode !== 'final-disposition-closure') {
            throw new Error('unexpected final disposition audit');
          }
          if (finalDisposition.raw_unresolved_count !== 146) throw new Error(`expected frozen raw disposition set of 146, got ${finalDisposition.raw_unresolved_count}`);
          if (finalDisposition.resolved_decisions !== 146 || finalDisposition.unresolved_count !== 0) {
            throw new Error('final disposition closure must resolve all 146 findings');
          }
          if (finalDisposition.reviewed_ledger.entry_count !== 23 || finalDisposition.reviewed_ledger.matched_count !== 23) {
            throw new Error('reviewed disposition ledger did not match 23/23 source statements');
          }
          if (finalDisposition.canonical_configuration_statements !== 19) {
            throw new Error(`expected 19 canonical configuration statements, got ${finalDisposition.canonical_configuration_statements}`);
          }
          if (finalDisposition.required_transforms !== 35) {
            throw new Error(`expected 35 AWS transforms, got ${finalDisposition.required_transforms}`);
          }
          if (finalDisposition.resolved_exclusions !== 92) {
            throw new Error(`expected 92 exclusions after configuration-evolution correction, got ${finalDisposition.resolved_exclusions}`);
          }
          if (finalDisposition.disposition_counts.KEEP_CANONICAL_CONFIGURATION !== 19) throw new Error('canonical configuration count mismatch');
          if (finalDisposition.disposition_counts.EXCLUDE_HISTORICAL_SCHEMA_RELEASE_ROW !== 56) throw new Error('historical schema-release exclusion count mismatch');
          if (finalDisposition.disposition_counts.EXCLUDE_HISTORICAL_BACKFILL_OR_REPAIR !== 26) throw new Error('historical backfill exclusion count mismatch');
          if (finalManifest.status !== 'READY_FOR_FINAL_OBJECT_RESOLUTION') throw new Error('final disposition manifest is not ready for final-object resolution');
          if (finalManifest.prohibited_historical_data_import !== true) throw new Error('final historical-data prohibition missing');
          if (finalManifest.identity_boundary?.version !== identity.version) throw new Error('identity boundary drifted during closure');

          if (replay.build !== 'AWS-V0-BUILD-3' || replay.mode !== 'final-object-reference-replay') throw new Error('unexpected final-object replay manifest');
          if (replay.status !== 'READY_FOR_EPHEMERAL_POSTGRES_REPLAY') throw new Error('final-object replay is not ready');
          if (replay.persistent_database_execution_allowed !== false || replay.aurora_execution_allowed !== false) throw new Error('ephemeral-only replay boundary missing');
          if (replay.expected_aws_transforms !== 35 || replay.counters.transform_source_statements !== 35) throw new Error('AWS transform-source count mismatch');
          if (replay.expected_canonical_configuration_statements !== 19 || replay.counters.canonical_configuration_statements !== 19) throw new Error('canonical configuration replay count mismatch');

          if (finalObjects.build !== 'AWS-V0-BUILD-3' || finalObjects.mode !== 'final-object-resolution') throw new Error('unexpected final-object resolution manifest');
          if (!['FINAL_OBJECTS_RESOLVED_AWS_TRANSFORMS_PENDING','FINAL_OBJECTS_RESOLVED'].includes(finalObjects.status)) throw new Error('final-object resolver did not resolve PostgreSQL reference schema');
          if (finalObjects.required_aws_transform_count !== 35) throw new Error('resolved object transform count mismatch');
          if (finalObjects.canonical_configuration_statement_count !== 19) throw new Error('resolved object configuration count mismatch');
          if (finalObjects.source_reference_schema?.schema_only !== true) throw new Error('reference schema is not schema-only');
          if (finalObjects.source_reference_schema?.ephemeral_compatibility_schema_excluded !== true) throw new Error('ephemeral auth shim was not excluded from schema dump');
          if (finalObjects.reference_object_counts.tables < 50 || finalObjects.reference_object_counts.routines < 150) throw new Error('resolved reference object counts unexpectedly low');
          if (finalObjects.baseline_emission_allowed !== false || finalObjects.aurora_execution_allowed !== false) throw new Error('final-object resolver opened deployment prematurely');

          console.log(JSON.stringify({
            migration_count: audit.migration_count,
            migration_range: audit.migration_range,
            hardened_unresolved: audit.unresolved_count,
            final_disposition_resolved: finalDisposition.resolved_decisions,
            final_disposition_unresolved: finalDisposition.unresolved_count,
            reviewed_ledger_matches: `${finalDisposition.reviewed_ledger.matched_count}/${finalDisposition.reviewed_ledger.entry_count}`,
            canonical_configuration_statements: finalDisposition.canonical_configuration_statements,
            required_transforms: finalDisposition.required_transforms,
            final_exclusions: finalDisposition.resolved_exclusions,
            replay_included_statements: replay.counters.included_statements,
            replay_excluded_statements: replay.counters.excluded_statements,
            reference_object_counts: finalObjects.reference_object_counts,
            final_object_blocker_count: finalObjects.blocker_count,
            final_object_blocker_codes: finalObjects.blockers.map((b) => b.code),
            final_object_status: finalObjects.status,
            identity_boundary: identity.version,
          }, null, 2));
          NODE

      - name: Prove baseline emission remains fail closed
        shell: bash
        run: |
          rm -f database/aws/0001_marketroute_aws_canonical_baseline.sql
          set +e
          node database/aws/compile-canonical-baseline.mjs --emit-baseline
          status=$?
          set -e
          if [ "$status" -ne 2 ] && [ "$status" -ne 3 ]; then
            echo "Expected fail-closed exit 2 or 3, received $status" >&2
            exit 1
          fi
          if [ -e database/aws/0001_marketroute_aws_canonical_baseline.sql ]; then
            echo "Compiler emitted baseline before AWS final-object transforms" >&2
            exit 1
          fi
          echo "Fail-closed baseline gate verified with exit $status."

      - name: Upload Build 3 audit and final-object artifacts
        uses: actions/upload-artifact@v4
        with:
          name: aws-v0-build3-schema-audit
          path: |
            database/aws/build3_schema_audit.json
            database/aws/0001_marketroute_aws_schema_manifest.json
            database/aws/build3_disposition_audit.json
            database/aws/0001_marketroute_aws_disposition_manifest.json
            database/aws/build3_final_disposition_audit.json
            database/aws/0001_marketroute_aws_final_disposition_manifest.json
            database/aws/BUILD3-REVIEWED-DISPOSITIONS.json
            database/aws/build3_final_object_replay.sql
            database/aws/build3_final_object_replay_manifest.json
            database/aws/build3_final_object_reference_schema.sql
            database/aws/build3_final_object_resolution_manifest.json
          if-no-files-found: error
          retention-days: 14
