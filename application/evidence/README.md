# Evidence application service

The application layer canonicalises raw source/evidence/claim inputs using the deterministic core and persists them through the Build-3 RPC-only database boundary.

There is no public HTTP API in Build 3 and no commercial authority is produced.
