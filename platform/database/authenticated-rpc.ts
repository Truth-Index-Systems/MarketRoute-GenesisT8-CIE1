function required(name:"SUPABASE_URL"|"SUPABASE_ANON_KEY"):string{const fallback=name==="SUPABASE_ANON_KEY"?process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY:undefined;const value=(process.env[name]??fallback)?.trim();if(!value)throw new Error(`MARKETROUTE_ENV_REQUIRED:${name}`);return value;}
export class AuthenticatedRpcClient{async call<T>(accessToken:string,functionName:string,payload:Record<string,unknown>):Promise<T>{if(!/^[a-z0-9_]+$/.test(functionName))throw new Error("MARKETROUTE_INVALID_RPC_NAME");const url=required("SUPABASE_URL").replace(/\/+$/,'');const anon=required("SUPABASE_ANON_KEY");const response=await fetch(`${url}/rest/v1/rpc/${functionName}`,{method:"POST",headers:{apikey:anon,Authorization:`Bearer ${accessToken}`,"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify(payload),cache:"no-store"});const raw=await response.text();let value:unknown=null;try{value=raw?JSON.parse(raw):null;}catch{value=raw;}if(!response.ok)throw new Error(`MARKETROUTE_AUTHENTICATED_RPC_FAILED:${functionName}`);return value as T;}}
