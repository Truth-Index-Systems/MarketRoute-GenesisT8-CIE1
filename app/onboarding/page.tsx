import { cookies } from "next/headers";import { redirect } from "next/navigation";import { sessionServiceFromEnvironment } from "@/application/session/service";import { ACCESS_COOKIE } from "@/app/app/_lib/session";import { MarketRouteLogo,Icon } from "@/ui";
export default async function Onboarding({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams;const jar=await cookies();const access=jar.get(ACCESS_COOKIE)?.value;if(!access)redirect("/login?next=/onboarding");try{const session=await sessionServiceFromEnvironment().authenticate(access);if(session.memberships.length>0)redirect("/app");}catch{redirect("/login?next=/onboarding");}const error=typeof query.error==="string"?decodeURIComponent(query.error):null;return <main className="mr-login"><div className="mr-app-grid" aria-hidden="true"/><section className="mr-login__panel"><a href="/" className="mr-login__brand"><MarketRouteLogo/></a><div className="mr-kicker"><span/> FIRST WORKSPACE</div><h1>Create your organisation.</h1><p>This becomes the tenant boundary for seller context, campaigns, research, opportunity authority and engagement.</p>{error&&<div className="mr-alert mr-alert--error"><Icon name="shield" size={16}/><span>{error}</span></div>}<form action="/api/session/onboarding" method="post" className="mr-login__form"><label><span>Organisation name</span><input name="name" required placeholder="Truth Index Systems"/></label><label><span>Workspace slug</span><input name="slug" pattern="[a-z0-9][a-z0-9-]{1,62}" required placeholder="truth-index-systems"/></label><button className="mr-button mr-button--primary" type="submit">Create workspace <Icon name="arrow" size={16}/></button></form></section></main>}
