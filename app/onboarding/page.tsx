import { cookies } from "next/headers";import { redirect } from "next/navigation";import { sessionServiceFromEnvironment } from "@/application/session/service";import { ACCESS_COOKIE } from "@/app/app/_lib/session";import { MarketRouteLogo,Icon } from "@/ui";
export default async function Onboarding({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){
  const query=await searchParams;const jar=await cookies();const access=jar.get(ACCESS_COOKIE)?.value;if(!access)redirect("/login?next=/onboarding");try{const session=await sessionServiceFromEnvironment().authenticate(access);if(session.memberships.length>0)redirect("/app");}catch{redirect("/login?next=/onboarding");}const error=typeof query.error==="string"?decodeURIComponent(query.error):null;
  return <main className="mr-login"><section className="mr-login__context"><a href="/"><MarketRouteLogo/></a><div><span>Workspace setup</span><h1>Give your commercial intelligence a home.</h1><p>This organisation becomes the private boundary around seller context, campaigns, research, opportunities and engagement.</p></div><footer>Step 1 of your MarketRoute workspace</footer></section><section className="mr-login__panel"><div className="mr-kicker"><span/> ORGANISATION</div><h2>Create your workspace</h2><p>Use the organisation name your team will recognise.</p>{error&&<div className="mr-alert mr-alert--error"><Icon name="shield" size={16}/><span>{error}</span></div>}<form action="/api/session/onboarding" method="post" className="mr-login__form"><label><span>Organisation name</span><input name="name" required placeholder="Truth Index Systems"/></label><label><span>Workspace URL</span><input name="slug" pattern="[a-z0-9][a-z0-9-]{1,62}" required placeholder="truth-index-systems"/></label><button className="mr-button mr-button--primary" type="submit">Create workspace <Icon name="arrow" size={16}/></button></form><small>You can change campaigns and research settings later. This only creates the organisation boundary.</small></section></main>
}
