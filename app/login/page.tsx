import { MarketRouteLogo,Icon } from "@/ui";
export default async function LoginPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams;const error=typeof query.error==="string"?query.error:null;const next=typeof query.next==="string"&&query.next.startsWith("/app")?query.next:"/app";return <main className="mr-login"><div className="mr-app-grid" aria-hidden="true"/><section className="mr-login__panel"><a href="/" className="mr-login__brand"><MarketRouteLogo/></a><div className="mr-kicker"><span/> SECURE WORKSPACE</div><h1>Enter MarketRoute.</h1><p>Sign in with your MarketRoute workspace account. Intelligence remains scoped to organisations you are an active member of.</p>{error&&<div className="mr-alert mr-alert--error"><Icon name="shield" size={16}/><span>{decodeURIComponent(error)}</span></div>}<form action="/api/session/login" method="post" className="mr-login__form"><input type="hidden" name="next" value={next}/><label><span>Email</span><input name="email" type="email" autoComplete="email" required/></label><label><span>Password</span><input name="password" type="password" autoComplete="current-password" required/></label><button className="mr-button mr-button--primary" type="submit">Open workspace <Icon name="arrow" size={16}/></button></form><small>Authentication is handled server-side through Supabase Auth. Service-role credentials are never exposed to the browser.</small></section></main>}
