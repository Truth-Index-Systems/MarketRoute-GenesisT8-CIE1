import { commandCentre,resolveCampaignId } from "@/app/app/_lib/data";import { workspaceSessionOrRedirect } from "@/app/app/_lib/session";import { applicationReadServiceFromEnvironment } from "@/application/read-model/service";import { asObjectArray,campaignListItem,companyProfile,statusTone } from "@/application/read-model/presentation";import { CampaignSwitcher,EmptyState,IntelligenceTable,PageHeader,Panel,StatusBadge } from "@/ui";
export default async function Companies({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const query=await searchParams;const {workspace}=await workspaceSessionOrRedirect();const cc=await commandCentre(workspace.organisationId);const campaigns=asObjectArray(cc.campaigns).map(campaignListItem);const campaignId=resolveCampaignId(cc,typeof query.campaign==="string"?query.campaign:null);const options=campaigns.map(c=>({campaignId:c.campaignId,name:c.name,workflowState:c.workflowState}));if(!campaignId)return <div><PageHeader eyebrow="COMPANIES" title="Research population" accent="by campaign." description="Every company stays tied to an explicit campaign scope."/><Panel><EmptyState icon="companies" title="No campaign available" body="Create or activate a campaign before companies can enter MarketRoute research."/></Panel></div>;const index=await applicationReadServiceFromEnvironment().companyIndex({organisationId:workspace.organisationId,campaignId,limit:200});const profiles=asObjectArray(index.companies).map(companyProfile);return <div><PageHeader eyebrow="COMPANIES" title="Every company," accent="with its real state." description="This list includes campaign-scoped companies whether they are actionable, blocked, stale or still under research." actions={<CampaignSwitcher campaigns={options} current={campaignId} action="/app/companies"/>}/><Panel>{profiles.length===0?<EmptyState icon="companies" title="No companies in this campaign" body="Genesis has not yet added companies to the campaign scope."/>:<IntelligenceTable head={["Company","Disposition","Research pressure","Truth","R4","R5","R6"]}>{profiles.map(p=><tr key={p.companyId}><td><a href={`/app/opportunities/${campaignId}/${p.companyId}`}><strong>{p.companyName}</strong><small>{p.canonicalDomain??"No canonical domain"}</small></a></td><td><StatusBadge compact label={p.disposition} tone={statusTone(p.disposition)}/></td><td>{p.researchPressure}</td><td>{Math.round(Number(p.truth.truthIndex??0))}</td><td>{p.commercialReality}</td><td>{p.routeAuthority}</td><td>{p.contactAuthority}</td></tr>)}</IntelligenceTable>}<div className="mr-table-footer">Showing {index.returnedCount} of {index.totalCount} campaign-scoped companies.</div></Panel></div>}
