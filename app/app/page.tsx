import { commandCentre } from "@/app/app/_lib/data";
import { workspaceSessionOrRedirect } from "@/app/app/_lib/session";
import { asObjectArray,campaignListItem,countFromObject,money } from "@/application/read-model/presentation";
import { EmptyState,Icon,MetricCard,PageHeader,Panel,SectionHeading,StatusBadge } from "@/ui";
export default async function CommandCentre(){const {workspace}=await workspaceSessionOrRedirect();const model=await commandCentre(workspace.organisationId);const campaigns=asObjectArray(model.campaigns).map(campaignListItem);const scoped=campaigns.reduce((a,c)=>a+c.scopedCompanies,0),opps=campaigns.reduce((a,c)=>a+c.materialisedOpportunities,0),actionable=campaigns.reduce((a,c)=>a+countFromObject(c.dispositionCounts,"ACTIONABLE"),0),reviewable=campaigns.reduce((a,c)=>a+countFromObject(c.workflowCounts,"REVIEWABLE"),0);return <div><PageHeader eyebrow="COMMAND CENTRE" title="Know what is" accent="actionable." description="A live view of what Genesis knows, which commercial realities are open, and where research pressure remains."/><section className="mr-metric-grid"><MetricCard label="Companies mapped" value={String(scoped)} meta="Campaign-scoped companies" icon={<Icon name="companies"/>}/><MetricCard label="Opportunities" value={String(opps)} meta="Materialised from authority-ready intelligence" icon={<Icon name="opportunities"/>}/><MetricCard label="Actionable now" value={String(actionable)} meta="Current R4 → R5 → R6" icon={<Icon name="shield"/>} accent/><MetricCard label="Awaiting review" value={String(reviewable)} meta="Founder decision required" icon={<Icon name="check"/>}/></section><Panel><SectionHeading eyebrow="Campaign intelligence" title="Active commercial programmes" description="Each campaign retains its own seller context, research budget and engagement policy."/>{campaigns.length===0?<EmptyState icon="campaigns" title="No campaigns yet" body="Create your first campaign through the platform setup flow before Genesis can research a market."/>:<div className="mr-live-card-list">{campaigns.map((c)=><a className="mr-live-card" href={`/app/campaigns/${c.campaignId}`} key={c.campaignId}><div><div className="mr-live-card__title"><strong>{c.name}</strong><StatusBadge compact label={c.workflowState} tone={c.workflowState==="ACTIVE"?"green":"slate"}/></div><p>{c.objectiveText??c.sellerName}</p></div><dl><div><dt>Companies</dt><dd>{c.scopedCompanies}</dd></div><div><dt>Actionable</dt><dd>{countFromObject(c.dispositionCounts,"ACTIONABLE")}</dd></div><div><dt>Reviewable</dt><dd>{countFromObject(c.workflowCounts,"REVIEWABLE")}</dd></div><div><dt>Research spent</dt><dd>{money(c.budget.spentTodayUsd)}</dd></div></dl></a>)}</div>}</Panel></div>}
