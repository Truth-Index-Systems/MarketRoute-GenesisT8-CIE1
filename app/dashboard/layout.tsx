import type { Metadata } from "next";

export const metadata:Metadata={title:"Founder Operations — MarketRoute",robots:{index:false,follow:false,nocache:true}};
export default function FounderDashboardLayout({children}:{children:React.ReactNode}){return children;}
