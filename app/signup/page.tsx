import { Icon,MarketRouteLogo } from "@/ui";

export default async function SignUpPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){
  const query=await searchParams;
  const error=typeof query.error==="string"?decodeURIComponent(query.error):null;
  return <main className="mr-login"><div className="mr-app-grid" aria-hidden="true"/><section className="mr-login__panel"><a href="/" className="mr-login__brand"><MarketRouteLogo/></a><div className="mr-kicker"><span/> SAVE YOUR MARKETROUTE</div><h1>Create your account.</h1><p>You have seen what MarketRoute produces. Create an account to build a private workspace, define your market and let Genesis research it for you.</p>{error&&<div className="mr-alert mr-alert--error"><Icon name="shield" size={16}/><span>{error}</span></div>}<form action="/api/session/signup" method="post" className="mr-login__form"><label><span>Email</span><input name="email" type="email" autoComplete="email" required/></label><label><span>Password</span><input name="password" type="password" autoComplete="new-password" minLength={8} required/></label><label><span>Confirm password</span><input name="passwordConfirmation" type="password" autoComplete="new-password" minLength={8} required/></label><button className="mr-button mr-button--primary" type="submit">Create MarketRoute account <Icon name="arrow" size={16}/></button></form><div className="mr-auth-switch"><span>Already have an account?</span><a href="/login">Sign in</a></div><small>Your account is created through Supabase Auth. Your workspace and market intelligence remain organisation-scoped.</small></section></main>;
}
