import { Icon,MarketRouteLogo } from "@/ui";

export default async function SignUpPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){
  const query=await searchParams;const error=typeof query.error==="string"?decodeURIComponent(query.error):null;
  return <main className="mr-login"><section className="mr-login__context"><a href="/"><MarketRouteLogo/></a><div><span>You have seen the output</span><h1>Now build MarketRoute around your market.</h1><p>Create a private account, then define the organisation and market you want Genesis to research.</p><ul><li>Organisation-scoped research and evidence</li><li>Commercial qualification before outreach</li><li>Current routes and contact authority</li></ul></div><footer>MarketRoute · by Truth Index Systems</footer></section><section className="mr-login__panel"><div className="mr-kicker"><span/> CREATE ACCOUNT</div><h2>Start your workspace</h2><p>Account creation is the first step to saving your own market intelligence.</p>{error&&<div className="mr-alert mr-alert--error"><Icon name="shield" size={16}/><span>{error}</span></div>}<form action="/api/session/signup" method="post" className="mr-login__form"><label><span>Email</span><input name="email" type="email" autoComplete="email" required/></label><label><span>Password</span><input name="password" type="password" autoComplete="new-password" minLength={8} required/></label><label><span>Confirm password</span><input name="passwordConfirmation" type="password" autoComplete="new-password" minLength={8} required/></label><button className="mr-button mr-button--primary" type="submit">Create account <Icon name="arrow" size={16}/></button></form><div className="mr-auth-switch"><span>Already have an account?</span><a href="/login">Sign in</a><span>·</span><a href="/preview">Return to product view</a></div><small>Your workspace and market intelligence remain organisation-scoped.</small></section></main>;
}
